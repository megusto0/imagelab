/* Минимальная заглушка fflate: функции преобразования строк. */
window.fflate = {
  strToU8(str) {
    const encoder = new TextEncoder();
    return encoder.encode(str);
  },
  strFromU8(u8) {
    const decoder = new TextDecoder();
    return decoder.decode(u8);
  },
};
