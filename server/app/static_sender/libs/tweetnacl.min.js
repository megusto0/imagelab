/* Заглушка TweetNaCl: реальная криптография выполняется через Web Crypto. */
window.nacl = {
  randomBytes(len) {
    const buf = new Uint8Array(len);
    crypto.getRandomValues(buf);
    return buf;
  },
};
